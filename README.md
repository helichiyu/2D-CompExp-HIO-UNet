# 对比实验：传统 HIO vs 未训练 UNet 相位恢复

> **七测（2026-07）**：大规模重复取统计——10 组 × 5 实验 = 50 个（HIO 5000 轮 ×3 + UNet 1500 轮 ×2）。核心发现：sigmoid 相变质量最好（曲线丝滑）、HIO 能恢复物体但长迭代几乎必然出周期斜线（C14，FFT 卷绕，文献确认）。详见 [七测汇报.md](七测汇报.md)、[实验猜想与结论.md](实验猜想与结论.md)。

在同一张图（567.png）上，严格控制变量对比**三种**二维相位恢复方法（均开直方图匹配 HM），并**多次独立重复取统计**（七测：10 组 × 5 实验 = 50 个）：

- **实验1·HIO**：传统严格 HIO（像空间硬替换振幅；relaxed HIO γ=0.9 防尺度爆炸）——每组 **5000 轮 × 3 次**（不同相位种子）。HIO 能恢复物体，但长迭代几乎必然出周期斜线（C14），故多次重复看统计 + 看图。
- **实验2·tanh+HIO**：未训练 UNet + tanh + HIO 反馈（UNet 输出做 FFT 取振幅，与 |A_orig| 算 MSE 反传；support 外用 HIO 反馈）——每组 1500 轮。
- **实验3·sigmoid+置0**：未训练 UNet + sigmoid + 置 0（同实验2 像空间；support 外置 0）——每组 1500 轮。

三实验**实空间约束完全一致**（正值 + 实数 + HM + shrinkwrap），差异仅在像空间处理、support 外策略、输出层。**评估前配准**（消除平移/共轭反转/符号的平凡歧义）。

---

## 背景

X 射线晶体学 / 相干衍射成像中，探测器只能测得傅里叶振幅 |F|，**相位丢失**（相位问题）。本实验从 567.png 的傅里叶振幅出发，丢掉相位，分别用三种方法恢复。

> 理论与文献见 [调研报告.md](调研报告.md)；实现设计见 [实现方案.md](实现方案.md)；实验历程见 [初测汇报.md](初测汇报.md)、[二测汇报.md](二测汇报.md)、[四测汇报.md](四测汇报.md)、[五测汇报.md](五测汇报.md)、[六测汇报.md](六测汇报.md)、[七测汇报.md](七测汇报.md)；猜想演变见 [实验猜想与结论.md](实验猜想与结论.md)。

---

## 环境

- **Python 解释器**：`D:\anaconda3\envs\use\python.exe`
- **依赖**（自实现 SSIM，无 skimage / cv2 依赖）：
  ```
  torch>=2.0.0
  numpy>=1.21.0
  pillow
  matplotlib
  ```
- GPU 可选（有 CUDA 自动用，无则 CPU）。

---

## 快速开始

```bash
D:\anaconda3\envs\use\python.exe main.py [HIO_ITER] [UNET_ITER] [RUN_DIR]
```

默认 `HIO_ITER=5000`、`RAAR_ITER=2000`、`UNET_ITER=2000`（=RAAR_ITER，控制变量对齐核心对照 P_M 实现）、`N_GROUPS=5`（5 组 × 4 法 = 20 个：HIO / tanh_full / RAAR / unet_raar）。第三参数 `RUN_DIR` 可选——不传则新建 `results/run_<时间戳>/`，传则**续跑**该目录（跳过已完成组）。

短测验证可传小值：

```bash
D:\anaconda3\envs\use\python.exe main.py 20 10     # 10 组 × 短轮次，约 4 分钟（验证逻辑 + 续跑）
```

产物写入 `results/run_<时间戳>/`：

- `config.txt` / `progress.json`（断点续跑进度）
- `group01..group10/`：每组 5 个实验（`hio_1/2/3` + `tanh_hio` + `sigmoid`），各自全套：
  - `real_space.png` / `spectra.png` / `support.png` / `convergence.png`
  - `metrics.csv`（末轮指标）、`history.csv`（**全程每次 eval** 的指标 + loss/振幅残差）
  - `state.pt`（末轮 best_rho + history，供离线复算）
  - 加 `hio_3runs.png`（HIO 3 次并排挑收敛）、`comparison_gXx.png/csv`（该组三方法对比）
- `summary.csv`：50 实验末轮指标汇总（组号 / 类型 / 实验 / 6 指标）

控制台每 20 轮（HIO 每 100 轮）打印一次指标。

---

## 测试建议（重要）

完整迭代耗时长，**强烈建议先用少轮次验证能跑通**，再放量：

1. **短测**：`main.py 20 10` 验证三实验跑通 + 全套出图；
2. **缓慢加轮次**看趋势（200 → 600 → 1200）；
3. 最后跑完整 `main.py` 出最终结果。

> 相位恢复 / 未训练网络对超参敏感，少轮次试探能大幅节省时间。四测发现 UNet 实验还有 **CUDA 非确定性**（同代码同 seed 跑两遍结果不同），单次结果有随机性，最后阶段需多次跑取统计。

---

## 项目结构

| 文件 | 作用 |
|---|---|
| `utils.py` | 甲乙共用工具：预处理、可微 FFT/IFFT、随机相位、shrinkwrap、直方图匹配、**实空间投影 `proj_S`**（RAAR 反射用，十测从 raar.py 提公共）、**配准 `register_to_gt`**、评估指标 `evaluate_all` |
| `hio.py` | 实验1：`run_hio()` 严格 HIO 迭代（β=0.7, γ=0.7） |
| `raar.py` | 九测：`run_raar()` RAAR 反射迭代（β=0.7，破 C14） |
| `dm.py` | 九测：`run_dm()` Difference Map 迭代（β=1.1，已排除） |
| `unet_pr.py` | `UNet` 类（输出层 sigmoid/tanh 可选）+ `run_unet()`（support 外置0/HIO反馈可选，`loss_scope` support/full 验 C16）+ `run_unet_raar()`（RAAR 骨架 + P_M 换 UNet 软约束，十测主菜） |
| `main.py` | 入口：读图 → 5 组 × 4 法（HIO / tanh_full / RAAR / unet_raar）→ 各出图 + 横向对比 + summary（支持断点续跑） |
| `smoke_test.py` | 初测验证脚本（短迭代） |
| `_probe_unet.py` | 二测 UNet 探测脚本 |
| `567.png` | 输入图（白背景 + 暗物体） |
| `调研报告.md` | 文献调研 + 30 篇参考文献 |
| `实现方案.md` | 函数级实现方案 |
| `各测汇报.md`（初测/二测/四测/五测/六测/七测） | 各轮实验过程、数据、发现 |
| `各测计划.md`（三测/六测/七测） | 三轮实验规划 |
| `实验猜想与结论.md` | 猜想与结论的严格区分（七测后修订） |

---

## 关键设计

1. **极性翻转（暗背景表示）**：内部统一 `ρ_work = bg_val − img`（物体亮、背景≈0）。`FFT(c−f)` 与 `FFT(f)` 非直流振幅相同，振幅约束不变，控制变量不破。

2. **保留直流**：相位恢复需要平均密度信息，去直流会导致不收敛。

3. **三实验像空间差异**（唯一变量）：实验1 硬替换振幅；实验2/3 UNet 输出做 FFT 取振幅、与 |A_orig| 算 MSE 反传。

4. **输出层参数化**：`OutConv` 激活可选——`sigmoid`（[0,1] 恒正，配 support 外置0）/ `tanh`（[−1,1] 可负，配 HIO 反馈）。

5. **support 外策略**：置 0（实验2，天然有界）/ **relaxed HIO** `γ·ρ−β·ρ′`（实验1/3，γ<1 防累加发散，γ=0.7；八测扫 γ 定位 0.7 优于 0.9）。

6. **shrinkwrap 动态支撑域**：高斯模糊 + 阈值二值化估物体区域，不偷看原图。σ 线性衰减（3.0→1.0），周期更新。

7. **直方图匹配**（Zhang-Main 300 等数量 bin）：参考直方图从原图统计，只在 support 内做。

8. **评估前配准**（★ 四测新增）：相位恢复有平凡歧义（平移/共轭反转/符号），未配准的 Δφ/amp_cc 被平移污染。`evaluate_all(align_to_gt=True)` 先在歧义群配准（FFT 互相关 + 翻转/符号枚举）再算指标。详见 [调研报告.md](调研报告.md) §5.1/§5.2。

9. **评估前投影**：ρ 投影到可行域 `clamp_min(0) × support_pred`，消除 support 外反馈值干扰。甲乙一致。

10. **取末轮**：`best_rho` / `best_point` 取末轮（收敛态），不取最优瞬间（cherry-pick，可能选不稳定峰值）。

11. **轮次、重复与断点续跑**（七测）：HIO 5000 轮 ×3 + UNet 1500 轮 ×2 为一组，10 组独立重复取统计（C12）；组间差异来自随机性（phase seed 不固定 + CUDA 非确定性 C6）。**断点续跑**：`progress.json` 记每组完成，中断后 `main.py <HIO_ITER> <UNET_ITER> results/run_xxx` 跳过已完成组。

---

## 评估指标（全套，配准后算）

| 指标 | 含义 | 反映 |
|---|---|---|
| PSNR | 峰值信噪比（实空间） | 像素误差（受尺度干扰） |
| SSIM | 结构相似（11×11 高斯窗） | **形态/结构（最贴人眼）** |
| Pearson CC | 线性相关（实空间） | 形态整体相关 |
| amp_cc | 振幅域相关 ΣF_o·F_c·cosΔφ/√(...) | 振幅谱拟合（可能和形态脱钩） |
| Δφ | 平均相位误差（arccos·cos 防卷绕） | 频域相位 |
| support IoU | 估 support 与真值的交并比 | support 位置 |

六指标**平等看待**，分析时全看。配准后 SSIM 多数情况贴形态，但 HIO"形态+周期斜线"场景会失真（R5），**必看 real_space.png**。详见 [调研报告.md](调研报告.md) §5。

---

## 七测状态（看图为准）

- **三方法命中率**（C12 统计性）：HIO 0/30（0%）、tanh+HIO 7/10（70% 但只"一点轮廓"、指标虚高）、sigmoid 2/10（20% 但质量最好）。
- **sigmoid 相变质量最好**（C13）：g6/g8 完全恢复 + 收敛曲线丝滑无震荡（psnr 18+）；UNet 平滑先验抑制高频卷绕条纹。
- **HIO 能恢复但长迭代必出周期斜线**（C2/C14 大推翻）：5000 轮 × 30 次每张中间都有物体，但被周期斜线淹没；斜线周期/倾角随机。主因 = HIO 负反馈震荡/stagnation（sigmoid 无反馈则丝滑无斜线为强证）；FFT 卷绕次要（padding 充足，已撤回"padding 不足"误判）。
- **指标虚高**（R5 强化）：iou/amp_cc 高的 tanh+HIO 也只"一点轮廓"；曲线丝滑才是真收敛信号。
- **HM 是双刃剑非元凶**（五测已证）：三实验都保留 HM。
- **CUDA 非确定性**（C6）：UNet 不可复现，多次跑取统计。

详见 [实验猜想与结论.md](实验猜想与结论.md)、[七测汇报.md](七测汇报.md)。后续（八测）：ER+HIO 交替（主攻 HIO 震荡）、多次重建对齐平均、PRTF 客观评估。

**九测状态（看图为准）**：矩阵扩成 5 法（HIO/RAAR/DM + tanh/sigmoid），5 组 × 5 实验。
- **RAAR 反射框架破 C14（C15）**：5/5 稳定 ssim 0.945–0.951、曲线丝滑、**背景干净无杂项**——项目首次拿到不靠概率的稳定高质量恢复。
- **DM β=1.1 排除**：5/5 ssim 0.10，本 setup 不工作。
- **γ=0.7 抑制 HIO 斜线**（C14 细化）：5000 轮无周期斜线，但指标仍震荡未收敛（γ 治斜线不治震荡）。
- **tanh/sigmoid UNet 通病（C16）**：中心物体质量高（部分超 RAAR），但背景斑杂/杂项多；RAAR 干净背景反衬此通病。
- **十测方向**：结合 RAAR（干净背景）+ UNet（中心质量）；tanh+HIO 试 γ=0.8。

详见 [九测汇报.md](九测汇报.md)。

**十测状态（看图为准）**：矩阵 4 法（HIO / tanh_full / RAAR / unet_raar），5 组 × 4 法 = 20 个；核心对照 P_M 实现（raar vs unet_raar）轮次对齐 2000。
- **unet_raar 命题失败（C18）**：UNet 软约束替代 P_M 引入不稳定性——5 组 ssim 0.619–0.938（vs RAAR 5/5 稳定 0.946–0.952），组4 在 1700 轮 ssim 0.926→0.640 突崩（loss 仍降、相位发散）；4/5 组中心 psnr 未超 RAAR。**RAAR 对 P_M 精确性敏感，近似投影不能替代硬替换。**
- **C16 归因成立（C17）**：tanh_full 全图 loss 背景比 support 内 loss 干净（loss 作用域是 C16 主因），代价是中心梯度被分摊（psnr 8.6–11.7）。
- **γ=0.8 折中失败**：HIO 5/5 ssim 0.215–0.292，与 γ=0.7 无改善，HIO 调参无救。
- **纯 RAAR 仍是当前最优基线**（C15），后续以 RAAR 为底。

详见 [十测汇报.md](十测汇报.md)。

**十一测状态（看图为准）**：矩阵 4 法 + Yoshida 自适应 σ，4 组 × 4 法 = 16 个（HIO 10000 / 其他 5000）。
- **自适应 σ 消除 unet_raar 突崩（C19）**：把 shrinkwrap 的 σ 从固定 schedule 改成 Yoshida 2024 自适应（support 剧变大 σ、稳定小 σ 锁定）。unet_raar 4/4 无中途突崩（min ssim 全在 it0-100 收敛前），vs 十测固定 σ 1/5 突崩。**防崩成功**。
- **但未提质**：unet_raar 4 组 ssim 0.77–0.93 仍低于 RAAR 0.94–0.96。RAAR 自适应 σ 下仍 4/4 稳（C15 保持）。
- **HIO 仍崩**：10000 轮 + 自适应 σ，4/4 ssim 0.21–0.23。C14 再证（support 改进救不了 HIO 震荡）。
- **自适应 σ 作为基础设施保留**（防崩、不破坏 RAAR）。后续提质从 loss 作用域下手（C17）。

详见 [十一测汇报.md](十一测汇报.md)。
