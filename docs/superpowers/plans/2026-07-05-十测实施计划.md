# 十测（UNet+RAAR 融合）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实施。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 实现 unet_raar（RAAR 反射骨架不变、P_M 换 UNet 软约束）+ tanh_full（全图 loss）两个新方法，跑 4 法 × 5 组矩阵，验证"RAAR 干净背景 + UNet 中心质量"能否兼得，并隔离 loss 作用域是否为 C16 背景斑杂根源。

**Architecture:** 三处代码改动——① `utils.py` 提公共 `proj_S`（实空间投影）；② `unet_pr.py` 的 `run_unet` 加 `loss_scope` 参数（support 内 / 全图），新增 `run_unet_raar`（RAAR 反射骨架 + P_M 换 UNet）；③ `main.py` 改 4 法矩阵（删 dm/sigmoid、GAMMA 0.7→0.8、加 unet_raar 槽位）。验证沿用项目惯例：import 检查 + probe 单跑看图 + main.py 短测 + 完整 5 组（无 pytest，项目无测试套件）。

**Tech Stack:** PyTorch（`torch.fft`）、numpy、matplotlib、Pillow。Python 解释器 `D:\anaconda3\envs\use\python.exe`。

## Global Constraints

- 中文代码注释、中文文档、中文 commit message（CLAUDE.md）。
- 不引入新依赖（仅 torch/numpy/PIL/matplotlib）。
- `results/` 已在 `.gitignore`（产物不上传）；probe 脚本入 git（参考 `raar_dm_probe.py`）。
- 看图为准（R8）、指标仅初筛（R5）；CUDA 非确定性（C6）→ 多组取统计。
- 关键参数：GAMMA=0.8（hio/tanh_full 用）、β=0.7（raar/unet_raar/tanh 反馈）、RAAR 2000 轮、HIO 5000 轮、UNet 类 1500 轮。
- 精准修改：只改本计划列出的行，不顺手改相邻代码（CLAUDE.md §3）。

## File Structure

| 文件 | 本次改动 | 职责 |
|---|---|---|
| `utils.py` | 加 `proj_S` | 公共实空间投影 P_S（raar.py / run_unet_raar 共享） |
| `raar.py` | 删私有 `_proj_S`、import `proj_S` | 纯 RAAR（`_proj_M` 保留私有，仅本处用） |
| `unet_pr.py` | `run_unet` 加 `loss_scope`；新增 `run_unet_raar` | UNet 路线 + 融合算法 |
| `main.py` | GAMMA=0.8、4 法矩阵、删 dm/sigmoid 槽位 | 编排层 |
| `unet_raar_probe.py` | 新建 | 三法单跑验证（RAAR 对照 + unet_raar + tanh_full） |
| `十测汇报.md` | 新建 | 实验汇报 |
| `实验猜想与结论.md` / `README.md` / `ARCHITECTURE.md` / `调研报告.md` | 更新 | 认知/文档同步 |

---

## Task 1: `utils.py` 提公共 `proj_S`

**Files:**
- Modify: `utils.py`（`ifft_real` 之后加 `proj_S`，约 L96 后）
- Modify: `raar.py:21-27,68`（删 `_proj_S`、改 import、调用处改名）

**Interfaces:**
- Produces: `utils.proj_S(x, support) -> Tensor`（实空间投影 P_S：support 外 0、内 clamp_min(0)，幂等）。`run_unet_raar`（Task 3）依赖它。

- [ ] **Step 1: `utils.py` 加 `proj_S`**

在 `utils.py` 的 `ifft_real` 函数（约 L89-95）之后、`# ===================== 随机相位初始化` 注释之前，插入：

```python
# ===================== 投影算子（RAAR 反射用）=====================
def proj_S(x, support):
    """实空间投影 P_S：support 外 = 0、support 内 clamp_min(0)。幂等。
    RAAR 反射 R_S = 2·P_S − I 用（raar.py / unet_pr.run_unet_raar 共享）。"""
    return torch.where(support > 0.5, torch.clamp_min(x, 0), torch.zeros_like(x))
```

- [ ] **Step 2: `raar.py` 改 import**

`raar.py:21-22` 现有：
```python
from utils import (fft_amp_phase, ifft_real, shrinkwrap_support, sigma_schedule,
                   histogram_match, evaluate_all)
```
改为：
```python
from utils import (fft_amp_phase, ifft_real, shrinkwrap_support, sigma_schedule,
                   histogram_match, evaluate_all, proj_S)
```

- [ ] **Step 3: `raar.py` 删私有 `_proj_S`、调用处改名**

删 `raar.py:25-27` 的 `_proj_S` 函数（保留 `_proj_M` 不动）：
```python
def _proj_S(x, support):
    """实空间投影 P_S：support 外 0、内 clamp_min(0)。幂等。"""
    return torch.where(support > 0.5, torch.clamp_min(x, 0), torch.zeros_like(x))
```

`raar.py:68` 改名：
```python
        r_s = 2 * proj_S(x, support) - x                       # R_S(x) = 2·P_S(x) − x
```
（原 `2 * _proj_S(x, support) - x`）

- [ ] **Step 4: 验证 import + RAAR 短跑不爆炸**

```bash
cd "e:/bm/迭代模型/对比"
D:/anaconda3/envs/use/python.exe -c "from utils import proj_S; from raar import run_raar; print('import ok')"
D:/anaconda3/envs/use/python.exe raar_dm_probe.py 200
```
预期：`import ok`；raar_dm_probe 跑通 200 轮、RAAR 数值不爆炸、real_space.png 有物体轮廓（回归验证：提公共后纯 RAAR 行为不变）。

- [ ] **Step 5: Commit**

```bash
git add utils.py raar.py
git commit -m "提 proj_S 到 utils 公共投影算子"
```

---

## Task 2: `run_unet` 加 `loss_scope` 参数

**Files:**
- Modify: `unet_pr.py:109-153`（签名加参数、loss 计算分支）

**Interfaces:**
- Produces: `run_unet(..., loss_scope='support')`。`loss_scope='full'` 时 loss 用 `|FFT(raw)|` 全图振幅（不乘 support）；`'support'`（默认）原样 `|FFT(raw*support)|`。Task 4 的 tanh_full、Task 5 的 main.py 用 `loss_scope='full'`。

- [ ] **Step 1: 签名加 `loss_scope`**

`unet_pr.py:109-113` 现有：
```python
def run_unet(amp_orig, rho_init, ref_edges, gt, support_gt,
             max_iter=5000, lr=1e-4, beta=0.7,
             out_act='tanh', use_hio_feedback=True,
             sigma0=3.0, sigma_end=1.0, sigma_interval=20, sigma_total=500,
             eval_every=20, unet_seed=None, align_eval=True, gamma=0.9):
```
改为（末尾加 `loss_scope='support'`）：
```python
def run_unet(amp_orig, rho_init, ref_edges, gt, support_gt,
             max_iter=5000, lr=1e-4, beta=0.7,
             out_act='tanh', use_hio_feedback=True,
             sigma0=3.0, sigma_end=1.0, sigma_interval=20, sigma_total=500,
             eval_every=20, unet_seed=None, align_eval=True, gamma=0.9,
             loss_scope='support'):
```

- [ ] **Step 2: docstring 补一行**

在 docstring（L114-124 区域，`use_hio_feedback` 说明后）加：
```python
      loss_scope:      'support'（默认，原样 fft(raw*support) 振幅）/ 'full'（fft(raw) 全图振幅，给 support 外补梯度，验 C16）
```

- [ ] **Step 3: loss 计算加分支**

`unet_pr.py:147-153` 现有：
```python
        # === 像空间（乙方唯一变量）：UNet 前向 → 振幅 MSE 反传（对照甲方硬替换）===
        raw = model(current_input)                              # ρ̃，输出层由 out_act 决定（sigmoid/tanh）
        rho_c = raw * support                                   # 候选密度，support 外置 0（振幅 loss / 最终输出 / 评估均用它）
        amp_pred = fft_amp_phase(rho_c)[0] / amp_max            # 振幅 loss 作用在 raw×support
        loss = mse(amp_pred, amp_orig_norm)
        loss.backward()
        optimizer.step()
```
改为：
```python
        # === 像空间（乙方唯一变量）：UNet 前向 → 振幅 MSE 反传（对照甲方硬替换）===
        raw = model(current_input)                              # ρ̃，输出层由 out_act 决定（sigmoid/tanh）
        if loss_scope == 'full':
            amp_pred = fft_amp_phase(raw)[0] / amp_max          # 全图振幅（C16 猜想：给 support 外补梯度）
        else:
            rho_c = raw * support                               # support 内（原样）：support 外置 0
            amp_pred = fft_amp_phase(rho_c)[0] / amp_max
        loss = mse(amp_pred, amp_orig_norm)
        loss.backward()
        optimizer.step()
```

- [ ] **Step 4: 验证两种 scope 都能短跑**

```bash
D:/anaconda3/envs/use/python.exe -c "
from utils import load_and_preprocess, fft_amp_phase, make_random_phase, init_density, shrinkwrap_support, estimate_reference_histogram
from unet_pr import run_unet
rho_work, bg_val, pad_info, _, _ = load_and_preprocess('567.png')
amp_orig, _ = fft_amp_phase(rho_work)
rho_init = init_density(amp_orig, make_random_phase(rho_work.shape, seed=42))
sgt = shrinkwrap_support(rho_work, 3.0)
ref = estimate_reference_histogram(rho_work, sgt, n_bins=300)
run_unet(amp_orig, rho_init, ref, rho_work, sgt, max_iter=50, eval_every=50, loss_scope='support')
run_unet(amp_orig, rho_init, ref, rho_work, sgt, max_iter=50, eval_every=50, loss_scope='full')
print('both scopes ok')
"
```
预期：两种 scope 各跑 50 轮、数值不爆炸、末尾打印 `both scopes ok`。

- [ ] **Step 5: Commit**

```bash
git add unet_pr.py
git commit -m "run_unet 加 loss_scope 参数（support/full）"
```

---

## Task 3: 新增 `run_unet_raar`（主菜）

**Files:**
- Modify: `unet_pr.py`（import 加 `proj_S`、文件末尾加 `run_unet_raar`）

**Interfaces:**
- Consumes: `utils.proj_S`（Task 1）、`UNet` 类（本文件）、`fft_amp_phase`/`shrinkwrap_support`/`sigma_schedule`/`histogram_match`/`evaluate_all`/`device`（本文件已 import）。
- Produces: `run_unet_raar(amp_orig, rho_init, ref_edges, gt, support_gt, max_iter=1500, lr=1e-4, beta=0.7, out_act='tanh', sigma0=3.0, sigma_end=1.0, sigma_interval=20, sigma_total=200, eval_every=20, unet_seed=None, align_eval=True, warmup=0) -> (rho_end, history)`。Task 4 probe、Task 5 main.py 调用它。

- [ ] **Step 1: import 加 `proj_S`**

`unet_pr.py:14-15` 现有：
```python
from utils import (fft_amp_phase, shrinkwrap_support, sigma_schedule,
                   histogram_match, evaluate_all, device)
```
改为：
```python
from utils import (fft_amp_phase, shrinkwrap_support, sigma_schedule,
                   histogram_match, evaluate_all, device, proj_S)
```

- [ ] **Step 2: 文件末尾加 `run_unet_raar`**

在 `run_unet` 函数之后（文件末尾）追加：

```python
# ===================== UNet + RAAR 融合（十测主菜）=====================
def run_unet_raar(amp_orig, rho_init, ref_edges, gt, support_gt,
                  max_iter=1500, lr=1e-4, beta=0.7,
                  out_act='tanh',
                  sigma0=3.0, sigma_end=1.0, sigma_interval=20, sigma_total=200,
                  eval_every=20, unet_seed=None, align_eval=True, warmup=0):
    """
    UNet + RAAR 融合：RAAR 反射骨架一字不改，P_M 从硬替换换成 UNet 软约束。
      像空间 P_M：UNet(r_s) 前向 → 全图振幅 MSE 反传训练（UNet 当软振幅投影器）。
      实空间 R_S / 反射组合 / β松弛：与纯 RAAR 逐字一致（背景干净的来源不变）。
    UNet 平滑先验顺带画清中心物体——目标：RAAR 干净背景 + UNet 中心质量（C15+C16 互补）。
    返回 (rho_end, history)，取末轮。详见 十测计划.md §3.1。
    """
    if unet_seed is not None:
        torch.manual_seed(unet_seed)
    model = UNet(out_act=out_act).to(device)
    optimizer = torch.optim.Adam(model.parameters(), lr=lr)
    mse = nn.MSELoss()

    amp_max = amp_orig.max().detach() + 1e-12
    amp_orig_norm = amp_orig / amp_max
    _, phase_orig = fft_amp_phase(gt)

    x = rho_init.detach().clone()
    support = shrinkwrap_support(rho_init, sigma0)

    metric_keys = ["psnr", "ssim", "pearson_cc", "amp_cc", "phase_err", "support_iou"]
    history = {"iter": [], "loss": [], "amp_residual": []}
    for k in metric_keys:
        history[k] = []
    start = time.time()
    for it in range(max_iter):
        # ① 实空间反射 R_S（no_grad，与纯 RAAR 一致）
        with torch.no_grad():
            r_s = 2 * proj_S(x, support) - x

        # ② P_M 换 UNet 软约束（带梯度，唯一改动）
        optimizer.zero_grad()
        raw = model(r_s.detach())                              # UNet 吃 r_s，tanh 输出 [-1,1]
        amp_pred = fft_amp_phase(raw)[0] / amp_max             # 全图振幅（对齐 P_M 全图 FFT 语义）
        loss = mse(amp_pred, amp_orig_norm)
        loss.backward()
        optimizer.step()

        # ③ 反射组合（no_grad，与纯 RAAR 一致）
        with torch.no_grad():
            proj_M_val = raw.detach()                          # UNet 输出顶替 P_M(r_s)
            r_m = 2 * proj_M_val - r_s                         # R_M = 2·P_M − r_s
            x_next = (beta / 2) * (r_m + x) + (1 - beta) * r_s  # β 松弛平均（β=0.7）
            x_next = histogram_match(x_next, support, ref_edges)
            if it >= warmup and it % sigma_interval == 0:      # 动态 support，沿用 RAAR 节奏
                sigma = sigma_schedule(it - warmup, sigma0, sigma_end, sigma_total)
                support = shrinkwrap_support(x_next, sigma)
            if it % eval_every == 0:
                m = evaluate_all(x_next, gt, amp_orig, phase_orig, support_gt, align_to_gt=align_eval)
                amp_res = ((fft_amp_phase(x_next)[0] - amp_orig).norm() / (amp_orig.norm() + 1e-12)).item()
                history["iter"].append(it)
                history["loss"].append(loss.item())
                history["amp_residual"].append(amp_res)
                for k in metric_keys:
                    history[k].append(m[k])
                elapsed = time.time() - start
                print(f"[UNet+RAAR] {it}/{max_iter} | loss {loss.item():.3e} | "
                      f"psnr {m['psnr']:.2f} | ssim {m['ssim']:.3f} | "
                      f"amp_cc {m['amp_cc']:.3f} | Δφ {m['phase_err']:.3f} | "
                      f"iou {m['support_iou']:.3f} | {elapsed:.1f}s")
            x = x_next.detach()

    return x_next, history
```

- [ ] **Step 3: 验证 import + 短跑不爆炸**

```bash
D:/anaconda3/envs/use/python.exe -c "
from utils import load_and_preprocess, fft_amp_phase, make_random_phase, init_density, shrinkwrap_support, estimate_reference_histogram
from unet_pr import run_unet_raar
rho_work, bg_val, pad_info, _, _ = load_and_preprocess('567.png')
amp_orig, _ = fft_amp_phase(rho_work)
rho_init = init_density(amp_orig, make_random_phase(rho_work.shape, seed=42))
sgt = shrinkwrap_support(rho_work, 3.0)
ref = estimate_reference_histogram(rho_work, sgt, n_bins=300)
run_unet_raar(amp_orig, rho_init, ref, rho_work, sgt, max_iter=100, eval_every=50)
print('unet_raar ok')
"
```
预期：跑 100 轮、数值不爆炸（ssim/psnr 是有限数）、末尾 `unet_raar ok`。
⚠️ 若数值爆炸（loss=nan / ssim 异常）：头号风险命中。回查 UNet 输入 `r_s` 值域（可加 `r_s = r_s / (r_s.abs().max()+1e-6)` 归一化试探），或降 β，调整后再验。

- [ ] **Step 4: Commit**

```bash
git add unet_pr.py
git commit -m "新增 run_unet_raar：RAAR 骨架 + P_M 换 UNet 软约束"
```

---

## Task 4: 写 `unet_raar_probe.py` + 单跑看图验证（人工 Gate）

**Files:**
- Create: `unet_raar_probe.py`

**Interfaces:**
- Consumes: `run_raar`（raar.py）、`run_unet(loss_scope='full')`、`run_unet_raar`（Task 3）、`dump_experiment`/`to_visual`（main.py）。同 `rho_init` 跑三法（控制变量）。

**说明：** 这是人工看图 gate。跑【纯 RAAR 对照 + unet_raar + tanh_full】三法同起点，看 compare.png 判断 unet_raar 是否收敛 + 中心清晰、tanh_full 背景是否比九测 tanh+HIO 干净。**看图通过才进 Task 5。**

- [ ] **Step 1: 写 `unet_raar_probe.py`**

```python
"""
unet_raar_probe.py —— 十测单跑验证：纯 RAAR 对照 + unet_raar + tanh_full（全图 loss）

同 rho_init、同轮次跑三法，各自 dump 全套 + 末轮并排 compare.png + 末轮指标表。验证：
  ① unet_raar 能跑通、数值不爆炸（头号风险）；
  ② unet_raar 中心物体是否比纯 RAAR 清晰 + 背景是否干净（C15+C16 互补命题）；
  ③ tanh_full（全图 loss）背景是否比九测 tanh+HIO（support 内 loss）干净（C16 根源猜想）。

用法：
  python unet_raar_probe.py [MAX_ITER]
  python unet_raar_probe.py 200        # 短测验证逻辑 + 数值不爆炸
  python unet_raar_probe.py 1500       # 默认 1500 轮，看趋势/看图

输出 results/unet_raar_<时间戳>/：raar/ + unet_raar/ + tanh_full/ 各全套 + compare.png
"""

import os
import sys
from datetime import datetime

import matplotlib.pyplot as plt

if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")

from utils import (load_and_preprocess, fft_amp_phase, make_random_phase, init_density,
                   shrinkwrap_support, estimate_reference_histogram, register_to_gt, unpad)
from raar import run_raar
from unet_pr import run_unet, run_unet_raar
from main import dump_experiment, to_visual

plt.rcParams['font.sans-serif'] = ['SimHei']
plt.rcParams['axes.unicode_minus'] = False

SIGMA0 = 3.0


def make_run_dir():
    run_dir = os.path.join('results', f"unet_raar_{datetime.now().strftime('%Y%m%d_%H%M%S')}")
    os.makedirs(run_dir, exist_ok=True)
    return run_dir


def plot_compare(gt_vis, runs, rho_work, bg_val, pad_info, save_path):
    """原图 + 各方法末轮并排。runs: [(label, best, hist), ...]"""
    n = len(runs)
    fig, axes = plt.subplots(1, n + 1, figsize=(5 * (n + 1), 5))
    axes[0].imshow(gt_vis, cmap='gray', vmin=0, vmax=1)
    axes[0].set_title('原图（真值）', fontsize=14); axes[0].axis('off')
    for i, (label, best, hist) in enumerate(runs):
        best_a = register_to_gt(best, rho_work)
        vis = unpad(bg_val - best_a, pad_info).squeeze().detach().cpu().numpy()
        axes[i + 1].imshow(vis, cmap='gray', vmin=0, vmax=1)
        axes[i + 1].set_title(f'{label}\nssim={hist["ssim"][-1]:.3f} psnr={hist["psnr"][-1]:.1f}',
                              fontsize=12)
        axes[i + 1].axis('off')
    plt.tight_layout(); plt.savefig(save_path, dpi=150, bbox_inches='tight'); plt.close()
    print(f"  已保存 compare.png")


def main(max_iter=1500, run_dir=None, phase_seed=None):
    run_dir = run_dir or make_run_dir()
    print(f"UNet+RAAR 单跑验证：{run_dir}/  轮次={max_iter}\n")
    with open(os.path.join(run_dir, 'config.txt'), 'w', encoding='utf-8') as f:
        f.write(f"MAX_ITER={max_iter}\nPHASE_SEED={phase_seed}\n"
                f"RAAR: beta=0.7（对照）\nunet_raar: beta=0.7, tanh\n"
                f"tanh_full: gamma=0.8, beta=0.7, tanh, loss_scope=full\n")

    rho_work, bg_val, pad_info, _, _ = load_and_preprocess('567.png')
    amp_orig, _ = fft_amp_phase(rho_work)
    support_gt = shrinkwrap_support(rho_work, SIGMA0)
    ref_edges = estimate_reference_histogram(rho_work, support_gt, n_bins=300)
    gt_vis = to_visual(rho_work, bg_val, pad_info)

    # 同一个 rho_init（控制变量：三法仅算法核/loss 不同）
    phase = make_random_phase(rho_work.shape, seed=phase_seed)
    rho_init = init_density(amp_orig, phase)

    runs = []
    print(f"\n--- RAAR 对照（β=0.7，{max_iter} 轮）---")
    best_r, hist_r = run_raar(amp_orig, rho_init, ref_edges, rho_work, support_gt,
                              max_iter=max_iter, beta=0.7, sigma0=SIGMA0, eval_every=100)
    dump_experiment('raar', 'RAAR 对照（β=0.7）', best_r, hist_r,
                    gt_vis, rho_work, bg_val, pad_info, support_gt, run_dir,
                    monitor_key='amp_residual', monitor_label='振幅残差')
    runs.append(('RAAR β=0.7', best_r, hist_r))

    print(f"\n--- unet_raar（β=0.7，{max_iter} 轮）---")
    best_u, hist_u = run_unet_raar(amp_orig, rho_init, ref_edges, rho_work, support_gt,
                                   max_iter=max_iter, beta=0.7, sigma0=SIGMA0, eval_every=100,
                                   out_act='tanh')
    dump_experiment('unet_raar', 'UNet+RAAR（β=0.7）', best_u, hist_u,
                    gt_vis, rho_work, bg_val, pad_info, support_gt, run_dir,
                    monitor_key='loss', monitor_label='UNet loss')
    runs.append(('unet_raar', best_u, hist_u))

    print(f"\n--- tanh_full（γ=0.8 全图 loss，{max_iter} 轮）---")
    best_t, hist_t = run_unet(amp_orig, rho_init, ref_edges, rho_work, support_gt,
                              max_iter=max_iter, lr=1e-4, sigma0=SIGMA0, eval_every=100,
                              out_act='tanh', use_hio_feedback=True, beta=0.7, gamma=0.8,
                              loss_scope='full')
    dump_experiment('tanh_full', 'tanh+全图loss（γ=0.8）', best_t, hist_t,
                    gt_vis, rho_work, bg_val, pad_info, support_gt, run_dir,
                    monitor_key='loss', monitor_label='UNet loss')
    runs.append(('tanh_full γ=0.8', best_t, hist_t))

    plot_compare(gt_vis, runs, rho_work, bg_val, pad_info, os.path.join(run_dir, 'compare.png'))

    print(f"\n===== 末轮指标对比 =====")
    print(f"{'方法':>16} | {'psnr':>7} | {'ssim':>6} | {'amp_cc':>7} | {'iou':>5}")
    print("-" * 56)
    for label, _, hist in runs:
        print(f"{label:>16} | {hist['psnr'][-1]:>7.2f} | {hist['ssim'][-1]:>6.3f} | "
              f"{hist['amp_cc'][-1]:>7.3f} | {hist['support_iou'][-1]:>5.3f}")

    print(f"\n完成。结果在 {run_dir}/  看 compare.png + 各 real_space.png")
    print(f"判断：unet_raar 中心是否比 RAAR 清晰 + 背景干净；tanh_full 背景是否比九测 tanh+HIO 干净")


if __name__ == '__main__':
    _max_iter = int(sys.argv[1]) if len(sys.argv) > 1 else 1500
    _run_dir = sys.argv[2] if len(sys.argv) > 2 else None
    main(_max_iter, _run_dir)
```

- [ ] **Step 2: 短跑验证不爆炸**

```bash
D:/anaconda3/envs/use/python.exe unet_raar_probe.py 200
```
预期：三法各 200 轮跑通、数值不爆炸、`results/unet_raar_<时间戳>/compare.png` 生成。

- [ ] **Step 3: 完整单跑 + 看图（人工 Gate）**

```bash
D:/anaconda3/envs/use/python.exe unet_raar_probe.py 1500
```
打开 `results/unet_raar_<时间戳>/compare.png` + 各 `real_space.png` + `convergence.png`，判断：
- **unet_raar**：收敛曲线是否丝滑（不震荡/不发散）、中心物体是否比纯 RAAR 清晰、背景是否干净。
- **tanh_full**：背景是否比九测 tanh+HIO（support 内 loss）干净（验 C16 猜想）。

⚠️ **看图通过才进 Task 5。** 若 unet_raar 不收敛/质量差：回到 Task 3 Step 3 的风险对策调整（β / 输入归一化 / 输出层），重跑本步。看图结论记录到后续 `十测汇报.md`。

- [ ] **Step 4: Commit（probe 入 git，产物 results/ 自动忽略）**

```bash
git add unet_raar_probe.py
git commit -m "新增 unet_raar_probe 三法单跑验证脚本"
```

---

## Task 5: `main.py` 改 4 法矩阵

**Files:**
- Modify: `main.py:50,63,250-262,281,299-365`（import、GAMMA、write_config、types、run_one_group）

**Interfaces:**
- Consumes: `run_unet_raar`（Task 3）、`run_unet(loss_scope='full')`（Task 2）。
- Produces: `main.py` 跑 4 法 × N 组矩阵（hio / tanh_full / raar / unet_raar），summary.csv 含 4 法。

- [ ] **Step 1: import 加 `run_unet_raar`**

`main.py:50` 现有：
```python
from unet_pr import run_unet
```
改为：
```python
from unet_pr import run_unet, run_unet_raar
```

- [ ] **Step 2: 删 `run_dm` import（dm 不再跑）**

`main.py:49` 现有：
```python
from dm import run_dm
```
删掉这一行（dm 槽位删除，import 不再用；按 CLAUDE.md 删因本次修改而废弃的 import）。

- [ ] **Step 3: `GAMMA` 0.7 → 0.8**

`main.py:63` 现有：
```python
GAMMA = 0.7        # relaxed HIO 松弛系数（support 外 γ·ρ − β·ρ′，γ<1 防累加发散）。八测阶段③扫 γ 发现 0.7 远优于七测 0.9（tanh+HIO psnr 16.46 vs 5.40、稳态震荡减半，C8 机理印证），故全局改 0.7
```
改为：
```python
GAMMA = 0.8        # relaxed HIO 松弛系数（support 外 γ·ρ − β·ρ′，γ<1 防累加发散）。九测 γ=0.7 抑制周期斜线但仍震荡、七测 γ=0.9 出斜线，十测试 0.8 折中
```

- [ ] **Step 4: `write_summary` 的 `types` 改 4 法**

`main.py:281` 现有：
```python
    types = [('hio', 'HIO'), ('raar', 'RAAR'), ('dm', 'DM'), ('tanh_hio', 'tanh+HIO'), ('sigmoid', 'sigmoid')]
```
改为：
```python
    types = [('hio', 'HIO'), ('tanh_full', 'tanh+全图loss'), ('raar', 'RAAR'), ('unet_raar', 'UNet+RAAR')]
```

- [ ] **Step 5: `write_config` 更新**

`main.py:250-262` 现有 `write_config` 函数体里：
```python
        f"RAAR β=0.7 / DM β=1.1（论文值，见 九测计划.md §3.5）",
        f"实验总数 = {n_groups * 5}（{n_groups} 组 × 5 法）",
```
改为：
```python
        f"RAAR β=0.7 / unet_raar β=0.7（论文值，见 十测计划.md §3）",
        f"tanh_full loss_scope=full（全图振幅，验 C16）",
        f"实验总数 = {n_groups * 4}（{n_groups} 组 × 4 法）",
```

- [ ] **Step 6: `main()` 的打印改 4 法**

`main.py:372` 现有：
```python
    print(f"轮次：HIO/DM={hio_iter}，RAAR={raar_iter}，UNet={unet_iter}，组数={n_groups}，"
          f"共 {n_groups * 5} 个实验\n")
```
改为：
```python
    print(f"轮次：HIO={hio_iter}，RAAR={raar_iter}，UNet类={unet_iter}，组数={n_groups}，"
          f"共 {n_groups * 4} 个实验\n")
```

- [ ] **Step 7: `run_one_group` 改 4 槽位**

`main.py:299-365` 整个 `run_one_group` 函数体替换为（删 dm 段、tanh_hio→tanh_full、删 sigmoid 段、加 unet_raar 段；hio/raar 段保留，hio 用 GAMMA=0.8 自动生效）：

```python
def run_one_group(g, run_dir, group_dir, shared, hio_iter, raar_iter, unet_iter):
    """跑一组 4 个实验：HIO / tanh_full（全图 loss）/ RAAR / unet_raar。
    HIO/RAAR 纯迭代各独立 phase；tanh_full/unet_raar 共享该组 rho_init（UNet 类）。
    各自 dump 全套，出该组 comparison_gXx.png/csv（4 法末轮对比，仅初筛；以 real_space.png 看图为准）。"""
    amp_orig = shared['amp_orig']; rho_work = shared['rho_work']
    ref_edges = shared['ref_edges']; support_gt = shared['support_gt']
    gt_vis = shared['gt_vis']; bg_val = shared['bg_val']; pad_info = shared['pad_info']

    # 该组 UNet 起点 rho_init（组间独立 → 组间样本独立）
    phase_g = make_random_phase(rho_work.shape)
    rho_init_g = init_density(amp_orig, phase_g)

    # HIO（纯迭代对照，relaxed γ=0.8）
    print(f"\n--- 组{g} HIO（{hio_iter} 轮，γ={GAMMA}）---")
    phase_h = make_random_phase(rho_work.shape)
    rho_init_h = init_density(amp_orig, phase_h)
    best_h, hist_h = run_hio(amp_orig, rho_init_h, ref_edges, rho_work, support_gt,
                             max_iter=hio_iter, beta=0.7, sigma0=SIGMA0, eval_every=100, gamma=GAMMA)
    dump_experiment(os.path.join(f'group{g:02d}', 'hio'), f'组{g} HIO',
                    best_h, hist_h, gt_vis, rho_work, bg_val, pad_info, support_gt, run_dir,
                    monitor_key='amp_residual', monitor_label='振幅残差')

    # tanh_full（UNet tanh + HIO 反馈 + 全图 loss）
    print(f"\n--- 组{g} tanh+全图loss（{unet_iter} 轮，γ={GAMMA}）---")
    best_t, hist_t = run_unet(amp_orig, rho_init_g, ref_edges, rho_work, support_gt,
                              max_iter=unet_iter, lr=UNET_LR, sigma0=SIGMA0, eval_every=100,
                              out_act='tanh', use_hio_feedback=True, beta=0.7, gamma=GAMMA,
                              loss_scope='full')
    dump_experiment(os.path.join(f'group{g:02d}', 'tanh_full'), f'组{g} tanh+全图loss',
                    best_t, hist_t, gt_vis, rho_work, bg_val, pad_info, support_gt, run_dir,
                    monitor_key='loss', monitor_label='UNet loss')

    # RAAR（反射算子，β=0.7）
    print(f"\n--- 组{g} RAAR（{raar_iter} 轮，β=0.7）---")
    phase_r = make_random_phase(rho_work.shape)
    rho_init_r = init_density(amp_orig, phase_r)
    best_r, hist_r = run_raar(amp_orig, rho_init_r, ref_edges, rho_work, support_gt,
                              max_iter=raar_iter, beta=0.7, sigma0=SIGMA0, eval_every=100)
    dump_experiment(os.path.join(f'group{g:02d}', 'raar'), f'组{g} RAAR',
                    best_r, hist_r, gt_vis, rho_work, bg_val, pad_info, support_gt, run_dir,
                    monitor_key='amp_residual', monitor_label='振幅残差')

    # unet_raar（UNet + RAAR 融合，β=0.7）
    print(f"\n--- 组{g} UNet+RAAR（{unet_iter} 轮，β=0.7）---")
    best_u, hist_u = run_unet_raar(amp_orig, rho_init_g, ref_edges, rho_work, support_gt,
                                   max_iter=unet_iter, lr=UNET_LR, sigma0=SIGMA0, eval_every=100,
                                   beta=0.7, out_act='tanh')
    dump_experiment(os.path.join(f'group{g:02d}', 'unet_raar'), f'组{g} UNet+RAAR',
                    best_u, hist_u, gt_vis, rho_work, bg_val, pad_info, support_gt, run_dir,
                    monitor_key='loss', monitor_label='UNet loss')

    # 该组 comparison（4 法末轮指标对比，仅初筛；以 real_space.png 看图为准 R5/R8）
    metrics_list = [(f'HIO g{g}', best_point(hist_h)),
                    (f'tanh+全图loss g{g}', best_point(hist_t)),
                    (f'RAAR g{g}', best_point(hist_r)),
                    (f'UNet+RAAR g{g}', best_point(hist_u))]
    plot_comparison(metrics_list, os.path.join(group_dir, f'comparison_g{g:02d}.png'))
    save_comparison_table(metrics_list, os.path.join(group_dir, f'comparison_g{g:02d}.csv'))
```

- [ ] **Step 8: 短测验证 4 法跑通**

```bash
D:/anaconda3/envs/use/python.exe main.py 100 50
```
预期：5 组 × 4 法短跑跑通、每组 `comparison_gXx.png/csv` 含 4 法、`summary.csv` 含 16 行（5 组 × 4 法 - 缺失）、控制台无报错。注：RAAR 仍跑 `RAAR_ITER=2000`（常量），短测会比 100/50 慢些（可接受；若想加速可临时把 `main.py:59` 的 `RAAR_ITER` 改 100，验证后改回 2000）。

- [ ] **Step 9: Commit**

```bash
git add main.py
git commit -m "main.py 改 4 法矩阵（删 dm/sigmoid、加 unet_raar、GAMMA=0.8）"
```

---

## Task 6: 完整 5 组跑

**Files:** 无代码改动（运行）。

- [ ] **Step 1: 完整跑**

```bash
D:/anaconda3/envs/use/python.exe main.py 5000 1500
```
预期：5 组 × 4 法 = 20 个实验全套（real_space/spectra/support/convergence.png + metrics.csv + history.csv + state.pt）+ 每组 comparison_gXx + summary.csv。
⚠️ 长实验，自主跑完（不中途确认）。单组出错用续跑重试该组：
```bash
D:/anaconda3/envs/use/python.exe main.py 5000 1500 results/run_<时间戳>
```

- [ ] **Step 2: 核验产物齐全**

检查 `results/run_<时间戳>/`：summary.csv 含 20 行、每组 4 个实验文件夹齐全。缺失则续跑补。

---

## Task 7: 看图 + 写汇报 + 更新认知文档

**Files:**
- Create: `十测汇报.md`
- Modify: `实验猜想与结论.md` / `README.md` / `ARCHITECTURE.md` / `调研报告.md`

- [ ] **Step 1: 看图下结论（R8 看图为准）**

看 `results/run_<时间戳>/` 各 `real_space.png` + `convergence.png` + summary.csv，回答：
1. **unet_raar**（主菜）：中心物体是否比纯 RAAR 清晰 + 背景是否干净（C15+C16 互补命题成立？）；收敛曲线是否丝滑。
2. **loss 作用域**（组2 tanh_full vs 九测 tanh+HIO）：全图 loss 背景是否更干净 → C16 是否归因到 loss 作用域（C17 候选）。
3. **γ=0.8 折中**（组1 HIO vs 九测 γ=0.7）：斜线/震荡是否改善。

- [ ] **Step 2: 写 `十测汇报.md`**

参考 `九测汇报.md` 结构：路线图表、矩阵参数、统计结果（各法 5 组 ssim 表）、看图结论、核心认知（C17/C18 候选）、后续方向。

- [ ] **Step 3: 更新认知文档**

- `实验猜想与结论.md`：新增 C17（loss 作用域是否 C16 根源，看 Step 1 结论）、C18（unet_raar 是否成立）；§4 指导后续补十测进展。
- `README.md`：矩阵改 4 法、`unet_pr.run_unet_raar`、`loss_scope` 参数、GAMMA=0.8、十测状态。
- `ARCHITECTURE.md`：模块表加 `run_unet_raar`、`utils.proj_S`；数据流图 4 法；已知问题补十测结论。
- `调研报告.md` §2.5：补 UNet 作 P_M 软约束的实测结论。

- [ ] **Step 4: Commit**

```bash
git add 十测汇报.md 实验猜想与结论.md README.md ARCHITECTURE.md 调研报告.md
git commit -m "十测汇报 + 认知文档更新（unet_raar / loss 作用域 / γ=0.8）"
```

---

## 验证标准（全计划）

- [ ] Task 1：`proj_S` 提公共后，纯 RAAR 短跑行为不变（回归验证）。
- [ ] Task 2：`run_unet` 两种 `loss_scope` 都能短跑不爆炸。
- [ ] Task 3：`run_unet_raar` 短跑不爆炸（头号风险未命中）。
- [ ] Task 4：unet_raar_probe 单跑看图通过（人工 gate）—— unet_raar 收敛 + 中心清晰、tanh_full 背景改善。
- [ ] Task 5：main.py 4 法短跑跑通，summary/comparison 含 4 法。
- [ ] Task 6：20 个实验全套 + summary.csv 齐全（断点续跑兜底）。
- [ ] Task 7：看图结论 + 十测汇报 + 认知文档更新。

## 风险对策（摘自十测计划 §7）

1. **unet_raar 近似 P_M 不收敛**（头号）：Task 3 Step 3 / Task 4 Step 3 看图把关；不收敛则调 β / r_s 归一化 / 输出层，重验；最坏退回纯 RAAR（C15 已成立）。
2. **r_s 值域怪**（带负/震荡）：Task 3 风险对策里加 `r_s` 归一化试探。
3. **全图 loss 分摊中心梯度**：Task 7 看图确认中心是否下降。
4. **γ=0.8 仍猜**：记录不强求（非主线）。
5. **指标仅初筛（R5/R8）**：全程以 real_space.png 看图 + 收敛曲线丝滑度为准。
